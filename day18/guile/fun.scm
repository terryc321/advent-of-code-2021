;; -*-scheme-*-


;; thought it be cons but end up with dotted pairs but input.txt is given as
;; list of 2 elements then nil as in (list 1 2)  -> 1 . (2 . nil)
(define addition (x y)
  (list x y))



